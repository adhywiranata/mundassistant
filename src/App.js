import React from 'react';
import { View, Text } from 'react-native';

import SplashScreen from './SplashScreen';

export default () => (
  <View style={{ flex: 1 }}>
    <SplashScreen />
  </View>
);
